#include "main.h"

/**
 * mul -> a function that multiplies two integers.
 *
 * @a: a is an integer argument
 * @b: b is an integer argument
 *
 * Return: multiplication of a and b
 */

int mul(int a, int b)
{
	return (a * b);
}
